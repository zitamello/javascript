const nameClient = 'John Doe'
let ageClient = 30;
let peso = 80;
const altura = 1.80;

const indiceMassaCorporal = peso / (altura * altura);

messageOutput = `O IMC do cliente ${nameClient} é ${indiceMassaCorporal}`;

console.log(messageOutput);
